import { Editor } from '@tiptap/react'
import React from 'react'

function FullscreenToolbar({ editor }: { editor: Editor }) {
    if (!editor) return null
  return (
    <div>
      
    </div>
  )
}

export default FullscreenToolbar
